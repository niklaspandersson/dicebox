# DiceBox Server Configuration
# Copy this file to .env and adjust values for your environment

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Port for the HTTP/WebSocket server (default: 3000)
PORT=3000

# Directory for static files (default: dist for production, use 'public' for development)
# STATIC_DIR=dist

# =============================================================================
# TURN SERVER CONFIGURATION (Optional)
# =============================================================================

# TURN server URLs (comma-separated for multiple servers)
# Required for WebRTC connections through restrictive NATs/firewalls
# Example: turn:turn.example.com:3478,turns:turn.example.com:5349
# TURN_URL=

# Option 1: Static credentials (simpler but less secure)
# TURN_USERNAME=
# TURN_CREDENTIAL=

# Option 2: Time-limited credentials (recommended for production)
# Shared secret for generating time-limited credentials (coturn --use-auth-secret)
# TURN_SECRET=

# Credential TTL in seconds (default: 86400 = 24 hours)
# TURN_TTL=86400

# =============================================================================
# REDIS CONFIGURATION (Optional)
# =============================================================================

# Redis is optional - the server uses in-memory storage if not configured
# Redis is recommended for production deployments with multiple server instances

# Redis server hostname
# REDIS_HOST=localhost

# Redis server port (default: 6379)
# REDIS_PORT=6379

# Redis password (if authentication is enabled)
# REDIS_PASSWORD=

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# Log level: trace, debug, info, warn, error, fatal (default: info)
LOG_LEVEL=info

# Set to 'true' for human-readable logs in development (default: JSON format)
# LOG_PRETTY=true
